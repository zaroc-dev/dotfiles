[project]
name = "ruzbyte-dotfiles"
version = "0.1.0"
description = "Installer tooling for ruzbyte dotfiles"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12",
]

[tool.uv]
package = false
